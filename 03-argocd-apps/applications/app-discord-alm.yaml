apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: discord-alm
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'discord-alm-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/rtaf-soc/rtarf-rke2-ml-addons.git
        targetRevision: "{{name}}" #Based on environment
        path: '03-argocd-apps/manifests/discord-alm'
      destination:
        name: in-cluster
        namespace: discord-alm
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
